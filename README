== Welcome to Jobline Authentication logic using Devise tool.

Ignored files:
* fm_config
* site_key
et.,.etc,.

Instructions:

1.) Configure necessary Gems (I normally used these): 
	gem 'devise', '1.1.rc2'
	gem 'mysql', '2.8.1'
	gem 'nifty-generators'
	gem 'lardawge-rfm'

2.) rake db:migrate to run devise modules

3.) Ask the users table SQL file from RH

4.) Dump the users table SQL file

5.) Restart server.